# Scatter Plot

Create a file called `exploration.py`. Read `yearly_avg_temp.csv` and make a scatter plot of the average maximum temperature (`avg_TX`) over the years.

Make sure to:

* Label the axes (`Year` and `Average max temperature (°C)`)
* Give the plot a title

## Run

    python exploration.py

## Checkpy

    checkpy exploration